{
    "engines": {
        "node": ">=12.0.0"
    },
    "name": "my-project",
    "scripts": {
        "start": "parcel src/index.html --no-source-maps --no-cache -p 3000",
        "build": "parcel src/index.html"
    },
    "devDependencies": {
        "parcel": "latest",
        "parcel-transformer-pug-precompile": "^1.0.6"
    }
}